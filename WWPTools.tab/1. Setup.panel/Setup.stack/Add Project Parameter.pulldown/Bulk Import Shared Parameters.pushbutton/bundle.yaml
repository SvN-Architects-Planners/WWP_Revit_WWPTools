title: "Bulk Import Shared Parameters"
tooltip: |
  Select multiple shared parameters or import editable rows from Excel, bulk-fill shared values across selected rows, then add the bindings to project categories
