<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Project Parameters"
        Height="320"
        Width="700"
        MinHeight="280"
        MinWidth="560"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        Background="#FFF7F8FA">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#FFD9DEE7" />
        <SolidColorBrush x:Key="TitleBrush" Color="#FF152033" />
        <SolidColorBrush x:Key="MutedBrush" Color="#FF5F6B7A" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF175CD3" />
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#FF1849A9" />
        <SolidColorBrush x:Key="PrimaryPressedBrush" Color="#FF12387A" />
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="SecondaryBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="SecondaryBorderBrush" Color="#FFC5CCD8" />
        <SolidColorBrush x:Key="SecondaryHoverBrush" Color="#FFF3F5F8" />
        <SolidColorBrush x:Key="SecondaryPressedBrush" Color="#FFE7EBF1" />

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="BorderBrush" Value="#FFC8D0DB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="MinHeight" Value="32" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="MinWidth" Value="80" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHoverBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryHoverBrush}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryPressedBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryPressedBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Foreground" Value="{StaticResource TitleBrush}" />
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryBorderBrush}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryHoverBrush}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryPressedBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="18"
                Margin="0,0,0,14">
            <StackPanel>
                <TextBlock Text="Import Project Parameters"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TitleBrush}" />
                <TextBlock Text="Specify the Excel workbook and Shared Parameters file — local paths or SharePoint/http URLs are both supported."
                           Margin="0,6,0,0"
                           TextWrapping="Wrap"
                           Foreground="{StaticResource MutedBrush}" />
            </StackPanel>
        </Border>

        <!-- File Inputs -->
        <Border Grid.Row="1"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="18"
                Margin="0,0,0,14">
            <StackPanel>
                <!-- Excel Workbook -->
                <TextBlock Text="Excel Workbook (.xlsx  —  local path or SharePoint URL)"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TitleBrush}"
                           Margin="0,0,0,6" />
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="ExcelPathBox"
                             Grid.Column="0" />
                    <Button x:Name="BrowseExcelButton"
                            Grid.Column="1"
                            Content="Browse"
                            Margin="8,0,0,0"
                            Style="{StaticResource SecondaryButtonStyle}" />
                </Grid>

                <!-- Shared Parameters File -->
                <TextBlock Text="Shared Parameters File (.txt  —  local path or SharePoint URL)"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TitleBrush}"
                           Margin="0,0,0,6" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="SharedParamPathBox"
                             Grid.Column="0" />
                    <Button x:Name="BrowseSharedParamButton"
                            Grid.Column="1"
                            Content="Browse"
                            Margin="8,0,0,0"
                            Style="{StaticResource SecondaryButtonStyle}" />
                </Grid>
            </StackPanel>
        </Border>

        <!-- Spacer -->
        <Grid Grid.Row="2" />

        <!-- Footer -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Style="{StaticResource SecondaryButtonStyle}" />
            <Button x:Name="OkButton"
                    Content="OK"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="0" />
        </StackPanel>
    </Grid>
</Window>
